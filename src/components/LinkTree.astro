---

interface Link {
  href: string;
  text: string;
  label: string;
}

interface Props {
    title: string;
    links: Link[];
}

const { title, links } = Astro.props;
---

<div>
    <h3>{title}</h3>
    <hr />

    <nav>
        { links.map( link => (<a href={link.href} aria-label={link.label} >{link.text}</a>)) }
    </nav>
</div>

<style>
    div {
        text-align: center;
        padding: 20px;
        padding-top: 10px;
    }

    nav {
        display: flex;
        flex-direction: column;
    }

    a {
        color: darkslategray;
        font-weight: bold;
        padding: 2px;
    }

    h3 {
        margin: 0;
    }

</style>

---
interface Items {
    text: string;
    url?: string;
}

interface Props {
    title?: string;
    items: Items[];
}

const { title, items } = Astro.props;
---

{title && <h2 class="text-2xl font-bold">{title}</h2>}

<ul>
    {
        items.map((item) => (
            <li class="list-disc ml-5">
                {item.url ? <a href={item.url} class="hover:underline">{item.text}</a> : item.text}
            </li>
        ))
    }
</ul>

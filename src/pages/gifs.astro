---
import { Image } from 'astro:assets';

// Import all GIF files from the 'images' folder
const gifImages = await Astro.glob('../media/gifs/*.gif');
---

{gifImages.map((image) => (
  <Image 
    src={image.default}
    alt={`GIF image`}
  />
))}

